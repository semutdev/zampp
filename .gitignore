build/bin
node_modules
frontend/dist

# Wails build output (executables, .app bundles, installers)
*.app
*.dmg
*.exe
build/bin/

# Engine bundle packaging (scripts/package-engine.sh) — staging workspace
# and any zip artefacts produced under build/. Never commit these.
build/.zampp-stage
build/.zampp
build/bin/*.zip

# Compiled binary produced by `wails build` at repo root
/zampp
